[target.x86_64-unknown-linux-gnu]
pre-build = [
    "apt-get update && apt-get install -y perl make gcc libasound2-dev"
]

[target.aarch64-unknown-linux-gnu]
pre-build = [
    "apt-get update && apt-get install -y perl make gcc libasound2-dev"
]
